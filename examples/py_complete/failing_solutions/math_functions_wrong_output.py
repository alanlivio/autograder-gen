#!/usr/bin/env python3
"""
Math Functions Module - WRONG OUTPUT VERSION
This version has correct signatures but produces wrong outputs - FAILED
"""

def add_numbers(a, b):
    """Add two numbers together - but returns wrong result."""
    return a - b  # WRONG: Should be a + b, this subtracts instead

def multiply(x, y):
    """Multiply two numbers - but returns wrong result."""
    return x + y  # WRONG: Should be x * y, this adds instead
