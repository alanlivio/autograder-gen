#!/usr/bin/env python3
"""
Math Functions Module - WRONG SIGNATURE VERSION
This version has incorrect function signatures that will cause FAILED
"""

def add_numbers(x, y, z):  # WRONG: Should be (a, b)
    """Add two numbers together - but wrong parameter names and count."""
    return x + y

def multiply(a):  # WRONG: Should be (x, y) with 2 parameters
    """Multiply - but missing second parameter."""
    return a * 2  # Just doubles the input
